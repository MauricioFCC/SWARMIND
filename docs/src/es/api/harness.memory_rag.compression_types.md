<!-- GENERADO POR scripts/gen_docs_api.py — NO editar a mano. Regenerar con `python scripts/gen_docs_api.py`. -->

# API — `harness.memory_rag.compression_types`

Compression types - Dataclasses and constants para compresion de prompts.

### `estimate\_tokens(text: str) -> int`

Estima tokens usando tiktoken o fallback a chars/4.

## `CompressionResult`

Resultado de una operacion de compresion.

## `TokenBudget`

Presupuesto de tokens por seccion.
