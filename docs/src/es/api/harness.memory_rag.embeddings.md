<!-- GENERADO POR scripts/gen_docs_api.py — NO editar a mano. Regenerar con `python scripts/gen_docs_api.py`. -->

# API — `harness.memory_rag.embeddings`

Embeddings — Funcion de embedding centralizada (FUENTE UNICA).

### `make\_embedding(text: str, dim: int = EMBEDDING\_DIM) -> np.ndarray`

Embedding deterministico basado en frecuencia de caracteres.
