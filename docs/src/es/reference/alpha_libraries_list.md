# Lista Completa de Librerias Cuantitativas — Contexto para Busqueda de Alpha

# ===== ALPHA GENERATION (senales predictivas) =====
QuantConnect(20.6k stars, C#/Python, event-driven backtesting + 100+ indicadores integrados),
Qlib-Microsoft(46.3k stars, AI-oriented quant platform, 20+ SOTA modelos ML),
FinRL-AI4Finance(15.7k stars, Financial RL, 5 DRL agents: A2C/DDPG/PPO/SAC/TD3),
FinRL-X(next-gen FinRL, desacoplado modular, produccion),
FinGPT(Financial LLM para analisis de sentimiento),
FinBERT(Financial BERT para sentiment analysis financiero),
Zipline(backtesting classico Python),
Zipline-Reloaded(Zipline modernizado),
VectorBT(backtesting vectorizado ultra-rapido),
PyPortfolioOpt(optimizacion de portafolio mean-variance, HRP, CLA),
Backtesting.py(backtesting ligero),
Backtrader(framework backtesting completo),
Hummingbot(market making crypto + CEX/DEX),
CCXT(unified API para 100+ exchanges crypto),
Stable-Baselines3(DRL: PPO/SAC/TD3/A2C/DQN - entrenar agentes RL),
TA-Lib(158+ indicadores tecnicos: RSI, MACD, Bollinger, patrones velas),
pandas-ta(80+ indicadores pandas-native),
SciPy(optimizacion numerica, senales, filtros),
Statsmodels(ARIMA, VAR, cointegracion, pruebas estacionariedad Q: ADF/KPSS),
Empyrical(metricas de riesgo/retorno: Sharpe, Sortino, Calmar, Drawdown),
PyFolio(analisis de portafolio),

# ===== FACTOR MINING Y ML AVANZADO =====
RD-Agent-Microsoft(auto-factor mining con LLMs, data-centric optimization),
Darts-Unit8(forecasting temporal: N-BEATS, TFT, Prophet, ARIMA),
Prophet-Meta(forecasting estacional con changepoints),
NeuralProphet(Prophet + neural networks),
Kats-Meta(analisis de series temporales: deteccion outliers, seasonality),
GreyKite-LinkedIn(forecasting jerarquico escalable),
Merlion-Salesforce(anomaly detection + forecasting unificado),
Orbit-Google(Bayesian structural time series),
Optuna(hyperparameter optimization automatico: TPE, CMA-ES, GP),

# ===== DEEP LEARNING / RL =====
PyTorch, JAX, TensorFlow,
Equinox(JAX-based neural nets),
Diffrax(JAX-based ODE/SDE solvers),
KAN(Kolmogorov-Arnold Networks - nueva frontera 2025-2026),
Torch-Geometric(GNN para graficos finansieros),
DeepGraphLibrary(GNN escalable),
PyGOD(GNN anomaly detection),

# ===== OPTIMIZACION CUANTICA =====
PennyLane(quantum ML),
Cirq-Google(quantum computing),
Qiskit-IBM(quantum computing),

# ===== HIGH-PERFORMANCE COMPUTING =====
Numba(JIT compiler para Python),
CuPy(GPU arrays - drop-in CUDA replacement),
RAPIDS(cuDF, cuML para GPU-accelerated pipelines),
Ray(compute distribuido),
Polars(DataFrame ultra-rapido en Rust),
DuckDB(OLAP query engine embebido),
LanceDB(vector store para memoria de agentes),
Dask(paralelismo en Python),

# ===== PIPELINES / ML-OPS / FEATURE STORE =====
MLflow(experiment tracking),
ZenML(pipeline management),
Feast(feature store open-source),
Evidently(monitoreo de modelos),
GreatExpectations(calidad de datos),
Airflow, Prefect, Dagster(orquestacion),
Kafka, Redpanda(streaming de eventos),

# ===== SERIES TEMPORALES / BASES DE DATOS =====
TimescaleDB(tiempo-real SQL),
QuestDB(columnar ultra-rapido),
ClickHouse(analytics columnar),
InfluxDB(series temporales),
Apache-Arrow, PyArrow(columnar in-memory),

# ===== NLP / LLMs / EMBEDDINGS =====
HuggingFace-Transformers, Sentence-Transformers,
BGE(embedding para RAG),
Voyage(embedding financiero),
Jina(embedding multi-modal),
Cohere(embedding + reranking),
GraphRAG(LightRAG, NanoGraphRAG, KAG - RAG sobre grafos),
Mem0, MemGPT, Letta(memoria persistente para agentes),

# ===== PROMPT ENGINEERING AVANZADO =====
DSPy(optimizacion automatica de prompts),
SGLang(structured generation),
Outlines(structured generation),
LMQL(query language para LLMs),
Guidance(structured prompting),

# ===== Swarmind FRAMEWORKS =====
AutoGen-Microsoft(multi-agent conversations),
CrewAI(agent teams),
LangGraph(agent workflows con estado),
Semantic-Kernel(agentes empresariales Microsoft),
OpenAI-Swarm(multi-agente ligero),
Camel(multi-agent role-playing),

# ===== MODELOS FRONTERA 2025-2026 =====
DeepSeek-V3, DeepSeek-R1, DeepSeek-Coder,
Llama-4, Llama-4-Scout, Llama-4-Maverick,
Mistral, Mixtral, Codestral, Mathstral,
Qwen-2.5, Qwen-VL,
Gemma-2, Phi-4,
GLM-5, InternLM,

# ===== EXCHANGE / BROKER CONEXION =====
CCXT(crypto universal API),
Alpaca(US stocks API),
Tradovate(futures API),
IBKR-API(Interactive Brokers),
Oanda(forex API),
Binance-API(crypto),
MT5-API(MetaTrader 5),
Onetick(high-frequency data),
Polygon(data market US),
Quandl(data financiero historico),
WRDS(data academico Wharton),

# ===== AREAS DE ALFA ESPECIFICO =====
Market-Microstructure(order book imbalance, VPIN, flow toxicity),
Statistical-Arbitrage(pairs trading, cointegracion, mean reversion),
Momentum(trend following, breakout, time-series momentum),
Sentiment(news, Twitter, Reddit, earnings calls, 10-K/10-Q NLP),
Options(greeks, implied volatility, volatility surface, skew),
FixedIncome(yield curve, duration, convexity, credit spreads),
Macro(CPI, NFP, PMI, GDP, interest rates, FX cross-asset),
ESG(carbon credits, green bonds, sustainability scores),
AlternativeData(satellite, credit card, web scraping, app downloads),
On-Chain(blockchain analytics, mempool, DEX flows, MEV),
Crypto-Finance(perpetual funding, basis trade, liquidation cascades),
Prediction-Markets(Polymarket, Kalshi - nueva frontera 2025-2026)
