# Exportacion y Backup

## Script Universal
python scripts/export_archive.py

Flags: --format zip, --output DIR, --dry-run, --verbose

## Session Log
python scripts/session_log.py add "titulo" -c "contenido" -d "dominio"
python scripts/session_log.py search "consulta"
python scripts/session_log.py last
